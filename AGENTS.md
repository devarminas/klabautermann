# AGENTS.md

## Layout

- `cmd/klabautermann/` — CLI and `--pipe` daemon.
- `internal/` — Go internals.
- `fixtures/` — fake screens, committed.
- `resources/` — private FGO pack, Go only.
- `ui/` — .NET solution (`Klabautermann.sln`, `Klabautermann.Application` with `DaemonClient`, no OpenCV).
